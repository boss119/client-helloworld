function add(a, b) {
    // let value1, value2;
    // if (!Number.isInteger(a) && !Number.isInteger(b)) {
    //     a = 0;
    //     b = 0;
    // } else if (!Number.isInteger(a)) {
    //     a = 0;
    // } else if (!Number.isInteger(b)) {
    //     b = 0;
    // } else {
    //     value1 = parseInt(a, 10);
    //     value2 = parseInt(b, 10);
    // }
    return a + b;
}

module.exports = add;
